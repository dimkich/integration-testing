package io.github.dimkich.integration.testing.execution;

/**
 * Marker interface representing a {@code null} or no-operation object
 * within the integration testing execution context.
 *
 * <p>Types implementing this interface can be used to explicitly denote
 * the absence of a value or behavior, instead of using {@code null}
 * references directly.</p>
 */
public interface Null {
}
